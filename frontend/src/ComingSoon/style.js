import styled from 'styled-components'

export const ComingSoonWrapper = styled.main`
    width: 100%;
    height: 100vh;
    padding: 5rem;
    background: linear-gradient(
        142.01deg,
        #ffe249 20.48%,
        rgba(251, 125, 8, 0.65) 90.5%
    );
    filter: drop-shadow(0px 4px 4px rgba(0, 0, 0, 0.25));
`
