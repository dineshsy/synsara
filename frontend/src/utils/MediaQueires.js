const device = {
    mobileS: '320px',
    mobileM: '375px',
    mobileL: '425px',
    tablet: '768px',
    laptop: '1024px',
    laptopL: '1440px',
    desktopM: '1920px',
    desktop: '2560px',
}

export const sizeMinW = {
    mobileS: `(min-width: ${device.mobileS})`,
    mobileM: `(min-width: ${device.mobileM})`,
    mobileL: `(min-width: ${device.mobileL})`,
    tablet: `(min-width: ${device.tablet})`,
    laptop: `(min-width: ${device.laptop})`,
    laptopL: `(min-width: ${device.laptopL})`,
    desktop: `(min-width: ${device.desktop})`,
    desktopM: `(min-width: ${device.desktopM})`,
    desktopL: `(min-width: ${device.desktop})`,
}

export const sizeMaxW = {
    mobileS: `(max-width: ${device.mobileS})`,
    mobileM: `(max-width: ${device.mobileM})`,
    mobileL: `(max-width: ${device.mobileL})`,
    tablet: `(max-width: ${device.tablet})`,
    laptop: `(max-width: ${device.laptop})`,
    laptopL: `(max-width: ${device.laptopL})`,
    desktop: `(max-width: ${device.desktop})`,
    desktopM: `(max-width: ${device.desktopM})`,
    desktopL: `(max-width: ${device.desktop})`,
}

export const sizeMinH = {
    mobileS: `(min-height: ${device.mobileS})`,
    mobileM: `(min-height: ${device.mobileM})`,
    mobileL: `(min-height: ${device.mobileL})`,
    tablet: `(min-height: ${device.tablet})`,
    laptop: `(min-height: ${device.laptop})`,
    laptopL: `(min-height: ${device.laptopL})`,
    desktop: `(min-height: ${device.desktop})`,
    desktopM: `(min-height: ${device.desktopM})`,
    desktopL: `(min-height: ${device.desktop})`,
}

export const sizeMaxH = {
    mobileS: `(max-height: ${device.mobileS})`,
    mobileM: `(max-height: ${device.mobileM})`,
    mobileL: `(max-height: ${device.mobileL})`,
    tablet: `(max-height: ${device.tablet})`,
    laptop: `(max-height: ${device.laptop})`,
    laptopL: `(max-height: ${device.laptopL})`,
    desktop: `(max-height: ${device.desktop})`,
    desktopM: `(max-height: ${device.desktopM})`,
    desktopL: `(max-height: ${device.desktop})`,
}
